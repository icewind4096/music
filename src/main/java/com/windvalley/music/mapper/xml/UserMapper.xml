<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.windvalley.music.mapper.UserMapper" >
<!--    <select id="getRolesByUserName" resultType="com.windvalley.music.entity.Role">-->
<!--        SELECT a.`id`, a.`name`, a.`title`, a.`create_time`, a.`update_time`-->
<!--        FROM `role` a-->
<!--            INNER JOIN `user_role` b ON a.id = b.role_id-->
<!--            INNER JOIN `user` c ON c.id = b.user_id-->
<!--        WHERE c.`username` = #{userName}-->
<!--    </select>-->
</mapper>
